% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_tooltip.R
\name{mod_tooltip}
\alias{mod_tooltip}
\title{Tooltip module}
\usage{
mod_tooltip(
  trigger = bsicons::bs_icon("info-circle"),
  type = c("tippy", "bslib", "bootstrap"),
  contents = "",
  size = NULL,
  style = NULL,
  ...
)
}
\arguments{
\item{trigger}{A \code{shiny.tag} (or plain text) that the tooltip attaches to.
Defaults to a small \code{bsicons::bs_icon("info-circle")}.}

\item{type}{Back-end to use.  One of \code{"tippy"} (default), \code{"bslib"}, or
\code{"bootstrap"}.}

\item{contents}{Character string — the tooltip body.  HTML is allowed for
\code{"tippy"} and \code{"bslib"}.}

\item{size}{Font-size passed through to the rendered element.  Accepts
any valid CSS value, e.g. \code{"0.8rem"}, \code{"12px"}.  \code{NULL} (default) leaves
the font size unchanged.}

\item{style}{Additional inline CSS string applied to the trigger wrapper,
e.g. \code{"color:#2c7bb6; cursor:help"}.  \code{NULL} (default) applies none.}

\item{...}{Extra arguments forwarded directly to the underlying
back-end function:
\itemize{
\item \code{"tippy"}     → \code{\link[tippy:tippy_funcs]{tippy::tippy()}}
\item \code{"bslib"}     → \code{\link[bslib:popover]{bslib::popover()}}
\item \code{"bootstrap"} → ignored (all options live in HTML attributes)
}}
}
\value{
A \code{shiny.tag} ready to embed in any UI.
}
\description{
A lightweight UI helper that wraps several tooltip back-ends behind a
single, consistent interface.  Drop it anywhere inside a UI or another
module — no paired server function is required.
}
\examples{
\dontrun{
# tippy tooltip on a custom trigger
mod_tooltip(shiny::tags$span("Hover me"), contents = "Hello tippy!")

# bslib popover
mod_tooltip(type = "bslib", contents = "A <b>bslib</b> popover")

# plain Bootstrap tooltip
mod_tooltip(type = "bootstrap", contents = "Bootstrap tooltip")
}

}
