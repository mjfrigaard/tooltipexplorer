% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_tooltip.R
\name{mod_tooltip}
\alias{mod_tooltip}
\title{Tooltip module}
\usage{
mod_tooltip(
  trigger = bsicons::bs_icon("info-circle"),
  type = c("bslib", "shinyhelper", "prompter", "shinyalert"),
  contents = "",
  size = NULL,
  style = NULL,
  helper_type = c("inline", "markdown"),
  helper_size = c("m", "s", "l"),
  alert_type = c("info", "success", "warning", "error"),
  ...
)
}
\arguments{
\item{trigger}{A \code{shiny.tag} (or plain text) that the tooltip attaches
to.  Defaults to a small \code{bsicons::bs_icon("info-circle")}.}

\item{type}{Back-end to use.  One of \code{"bslib"} (default),
\code{"shinyhelper"}, \code{"prompter"}, or \code{"shinyalert"}.}

\item{contents}{Character string or vector — the tooltip/popover/modal
body.  HTML is accepted for \code{"bslib"}.  For \code{"shinyhelper"} a
plain-text character vector is preferred — elements are joined with
\verb{<br>} by \code{helper(type = "inline")}, so embedding HTML tags causes
double-escaping.  Ignored as plain text by \code{"prompter"}.  For
\code{"shinyalert"} HTML is supported (rendered via \code{html: true}).}

\item{size}{CSS font-size for the trigger wrapper span, e.g.
\code{"0.85rem"}.  \code{NULL} (default) leaves the font size unchanged.  Note:
this is purely a CSS property on the wrapper — it is distinct from
\code{helper_size} (the shinyhelper modal size).}

\item{style}{Additional inline CSS for the trigger wrapper, e.g.
\code{"color:#6c757d"}.  \code{NULL} (default) applies none.}

\item{helper_type}{Content type forwarded to \code{shinyhelper::helper()} as
its \code{type} argument.  One of \code{"inline"} (default) or \code{"markdown"}.
Ignored by all other back-ends.}

\item{helper_size}{Modal size forwarded to \code{shinyhelper::helper()} as its
\code{size} argument.  One of \code{"s"}, \code{"m"} (default), or \code{"l"}.  Ignored by
all other back-ends.  Kept separate from \code{size} to avoid the two params
colliding.}

\item{alert_type}{Alert style forwarded to \code{shinyalert()} as its \code{type}
argument.  One of \code{"info"} (default), \code{"success"}, \code{"warning"}, or
\code{"error"}.  Ignored by all other back-ends.}

\item{...}{Extra arguments forwarded directly to the underlying
back-end function:
\itemize{
\item \code{"bslib"}       → \code{\link[bslib:popover]{bslib::popover()}}  (e.g. \code{title}, \code{placement})
\item \code{"shinyhelper"} → \code{shinyhelper::helper()}  (e.g. \code{title}, \code{colour},
\code{icon}, \code{buttonLabel}, \code{easyClose}, \code{fade})
\item \code{"prompter"}    → \code{prompter::add_prompt()}  (e.g. \code{position},
\code{rounded}, \code{bounce}, \code{arrow}, \code{animate})
\item \code{"shinyalert"}  → stored as \verb{data-sa-*} attributes; recognised keys
are \code{title} and \code{confirmButtonText}
}}
}
\value{
A \code{shiny.tag} (or \code{shiny.tagList}) ready to embed in any UI.
}
\description{
A lightweight UI helper that wraps several tooltip/popover/modal back-ends
behind a single, consistent interface.  Drop it anywhere inside a UI or
another module — no paired server function is required.
}
\section{\code{shinyalert} usage in UI}{

\code{type = "shinyalert"} requires a delegated \code{jQuery} click handler injected
into the page. See vignette for more information:

I.e., in \code{app_ui.R}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{shiny::tags$script(shiny::HTML("
  $(document).on('click', '[data-sa-title]', function() \{ ... \});
"))
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# bslib popover (click to open)
mod_tooltip(
  type     = "bslib",
  contents = "A <b>bslib</b> popover with HTML.",
  title    = "More info"
)

# shinyhelper inline help modal
mod_tooltip(
  trigger     = shiny::tags$span("Sharpe Ratio"),
  type        = "shinyhelper",
  contents    = c("Sharpe Ratio", "Assumes a zero risk-free rate."),
  helper_type = "inline",
  helper_size = "m",
  title       = "Sharpe Ratio"
)

# prompter attribute-driven tooltip
mod_tooltip(
  trigger  = bsicons::bs_icon("info-circle"),
  type     = "prompter",
  contents = "Number of trading days in the rolling window.",
  position = "right"
)

# shinyalert modal on click (requires sa-handler to be present in UI)
mod_tooltip(
  trigger    = shiny::tags$span("What is ann. vol?"),
  type       = "shinyalert",
  contents   = "Annualised volatility = SD of daily log returns x sqrt(252).",
  title      = "Annualised Volatility",
  alert_type = "info"
)
}

}
