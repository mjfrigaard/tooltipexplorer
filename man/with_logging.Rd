% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/with_logging.R
\name{with_logging}
\alias{with_logging}
\title{Execute an expression with structured error and warning logging}
\usage{
with_logging(expr, context = "", ns = "tooltipexplorer/app")
}
\arguments{
\item{expr}{Expression to evaluate (passed unevaluated via \code{...}).}

\item{context}{Short string identifying the call site, e.g.
\code{"mod_outputs / prices_r"}.  Prepended to every log message.}

\item{ns}{Logger namespace string.
Defaults to \code{"tooltipexplorer/app"}.}
}
\value{
The value of \code{expr} on success; re-throws on error.
}
\description{
Wraps \code{expr} in a \code{tryCatch()} that:
\itemize{
\item logs warnings via \code{\link[logger:log_level]{logger::log_warn()}} and re-issues them so Shiny can
also handle them
\item logs errors via \code{\link[logger:log_level]{logger::log_error()}} and re-throws so the caller /
Shiny sees the error normally
}
}
\examples{
\dontrun{
result <- with_logging(
  context = "my_module / compute",
  ns      = "tooltipexplorer/app",
  sqrt(4)
)
}

}
